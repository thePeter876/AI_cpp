#include "timer.h"



/*Timer::Timer()
{
	this->startMoment = std::chrono::steady_clock::now();
	this->counting = true;
}*/

/*void Timer::start() {
	this->startMoment = std::chrono::steady_clock::now();
	this->counting = true;
}*/

/*__int64 Timer::stop() {
	this->counting = false;
	//std::chrono::steady_clock::time_point end = std::chrono::steady_clock::now();
	//std::chrono::steady_clock::time_point s = this->start;
	this->currentCount = std::chrono::duration_cast<std::chrono::milliseconds>
		(std::chrono::steady_clock::now() - std::chrono::steady_clock::time_point(this->startMoment)).count();
	return this->currentCount;
}*/

/*__int64 Timer::getTimeElapsed() {
	this->currentCount = std::chrono::duration_cast<std::chrono::milliseconds>
		(std::chrono::steady_clock::now() - std::chrono::steady_clock::time_point(this->startMoment)).count();
	return this->currentCount;
}*/

